import React from 'react';
import {View} from 'react-native';
import TopNav from '../../navigations/BooingNavigation';

const ServicesScreen = ({navigation}) => {
  return (
    <View style={{flex: 1}}>
      <TopNav />
    </View>
  );
};

export default ServicesScreen;
